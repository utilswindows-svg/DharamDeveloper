# Server
NODE_ENV=development
PORT=5000

# MySQL
DB_HOST=127.0.0.1
DB_PORT=3306
DB_NAME=feedback_db
DB_USER=root
DB_PASSWORD=root

# JWT (used to protect admin routes)
JWT_SECRET=change_me_super_secret

# CORS
CORS_ORIGIN=*

# Rate limit (per IP)
FEEDBACK_RATE_WINDOW_MS=900000
FEEDBACK_RATE_MAX=20